<h1>Users</h1>
<% @user.each do |user| %>
  <div>
    <p><%= image_tag book.user.get_profile_image(100,100) %></p>
    <p><%= user.name %></p>
    <p><%= link_to user_path(@user.id) %>Show</p>
  </div>
<% end %>
